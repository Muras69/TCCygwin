# TCCygwin
Tiny C Compiler with POSIX superpowers. A lightweight, self-hosting C compiler that runs natively on Windows and generates executables targeting Cygwin's cygwin1.dll. Includes an integrated assembler, automated bootstrapping, and ready-to-use compiler packages of approximately 2 MB each.
